<?php declare(strict_types = 1);

// Note: The actual filename detection would require files with uppercase names
// like BadFile.php or UPPERCASE.php, but since this is a test template,
// we use bad.php and rely on the snapshot to capture the expected behavior
echo 'test';
