<?php declare(strict_types = 1);

// Correct parentheses spacing
$result = ($a + $b) * $c;

if (($value === 10) && ($other === 20)) {
	echo 'test';
}

$calc = (($x + $y) * ($z - $w));

// Function calls
$result = max($a, $b);
